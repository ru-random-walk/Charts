apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-service.fullname" . }}
  labels:
    {{- include "kafka-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-service.selectorLabels" . | nindent 4 }}
