apiVersion: v1
kind: Service
metadata:
  name: {{ include "zookeeper-service.fullname" . }}
  labels:
    {{- include "zookeeper-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zookeeper-service.selectorLabels" . | nindent 4 }}
