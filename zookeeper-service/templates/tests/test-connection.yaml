apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zookeeper-service.fullname" . }}-test-connection"
  labels:
    {{- include "zookeeper-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zookeeper-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
